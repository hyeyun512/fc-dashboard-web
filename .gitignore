node_modules
.next
out
.env
.env.local
.vercel
*.log
*.tsbuildinfo

.vercel
.env*

# 엑셀이 열려 있는 동안 만드는 임시 잠금 파일
~$*.xlsx

# 메일 붙여넣기용으로 뽑는 시트 이미지 (매달 다시 만들면 되므로 저장소에 넣지 않는다)
보고 이미지/
# 보고 메일 원본 (개인 정보가 들어 있다)
*.msg
